<Window x:Class="Insulter.Dialog.FetchInsultDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:fa="http://schemas.fontawesome.io/icons/" Title="Fetch Insults" Height="240" Width="360" ResizeMode="NoResize" WindowStyle="ToolWindow" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center" Grid.Row="0">
            <TextBlock Text="Language:" VerticalAlignment="Center" Width="80" />
            <ComboBox Width="200" ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8" Grid.Row="1">
            <TextBlock Text="Amount:" VerticalAlignment="Center" Width="80" />
            <TextBox Width="200" Text="{Binding Amount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="0,8,0,0">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="Cancel_Click" IsEnabled="{Binding IsNotLoading}" />
            <Button Content="Fetch" Width="90" Click="Fetch_Click" IsEnabled="{Binding IsNotLoading}" />
        </StackPanel>
        <!-- Loading overlay -->
        <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" Background="#AA000000" Grid.RowSpan="3">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <fa:ImageAwesome Icon="Spinner" Spin="True" SpinDuration="2" Foreground="White" Height="48" Width="48" />
                <TextBlock Text="{Binding LoadingMessage}" Foreground="White" FontSize="14" Margin="0,10,0,0" TextAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>