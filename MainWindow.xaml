<Window x:Class="Insulter.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:Insulter" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" mc:Ignorable="d" xmlns:fa="http://schemas.fontawesome.io/icons/" Title="Insulter" Height="450" Width="800" Style="{StaticResource MaterialDesignWindow}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Margin="10" Padding="10">
            <TextBlock Text="{Binding Insult}" Margin="10" Padding="10" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Center" Orientation="Vertical" Margin="10">
            <Button Content="Insult Me" Style="{StaticResource MaterialDesignRaisedDarkButton}" FontFamily="/Resources/Roboto/Roboto-BoldItalic.ttf#Roboto" Width="auto" Padding="5" Margin="10" BorderBrush="White" BorderThickness="1" Click="GetInsultFromDB_Click" />
            <Button Content="Fetch Insult" Style="{StaticResource MaterialDesignRaisedDarkButton}" FontFamily="/Resources/Roboto/Roboto-BoldItalic.ttf#Roboto" Width="auto" Padding="5" Margin="10" BorderBrush="White" BorderThickness="1" Click="FetchInsult_Click" />
        </StackPanel>
        <Grid Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <fa:ImageAwesome Icon="Spinner" Spin="True" SpinDuration="2" Foreground="White" Height="64" Width="64" />
                <TextBlock Text="Fetching Insults..." Foreground="White" FontSize="24" Margin="10" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
